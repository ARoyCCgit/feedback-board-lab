# Copy this file to `.env.local` and fill in a real value.
# Never commit `.env.local` to version control.

# Secret used to authorize DELETE requests to /api/feedback.
# The DELETE handler expects an `Authorization: Bearer <ADMIN_KEY>` header.
ADMIN_KEY=replace-with-a-strong-random-secret
